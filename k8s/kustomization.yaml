apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - mongodb.yaml        # 1️⃣ deploy MongoDB first
  - bigchaindb.yaml     # 2️⃣ BigchainDB depends on MongoDB
  - redis.yaml          # 3️⃣ Redis (independent)
  - reactivechaindb.yaml # 4️⃣ depends on BigchainDB + Redis

